coverage:
  status:
    project:
      default:
        target: 65%
        threshold: 2%
    patch:
      default:
        target: 80%

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true

ignore:
  - "tests/**/*"
  - "examples/**/*"
  - "**/__init__.py"
